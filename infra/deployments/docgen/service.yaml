

apiVersion: v1
kind: Service
metadata:
  name: docgen-service
  namespace: docgen
spec:
  type: ClusterIP
  ports:
    - port: 5555         
      targetPort: 5555  
      protocol: TCP
  selector:
    app: docgen    # Must match your Deployment's labels
